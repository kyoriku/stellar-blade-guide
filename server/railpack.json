{
  "steps": {
    "install": {
      "cmds": [
        "uv sync --locked --no-dev --no-install-project"
      ]
    },
    "build": {
      "cmds": []
    },
    "deploy": {
      "startCmd": "uv run uvicorn main:app --host 0.0.0.0 --port $PORT --proxy-headers"
    }
  }
}