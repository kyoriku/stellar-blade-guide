{
  "name": "stellar-blade-guide-v2",
  "version": "2.0.0",
  "description": "Stellar Blade guide with React/TypeScript frontend, Python/FastAPI backend, and PostgreSQL database.",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && uv run uvicorn app.main:app --host 0.0.0.0 --reload",
    "client": "cd client && npm run dev",
    "install": "cd client && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && uv run uvicorn app.main:app --host 0.0.0.0",
    "e2e": "cd e2e && npx playwright test",
    "e2e:install": "cd e2e && npm ci && npx playwright install chromium"
  },
  "keywords": [],
  "author": "",
  "private": true,
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}