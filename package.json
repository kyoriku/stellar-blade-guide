{
  "name": "stellar-blade-guide-v2",
  "version": "2.0.0",
  "description": "Stellar Blade guide with React/TypeScript frontend, Python/FastAPI backend, and PostgreSQL database.",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && source .venv/bin/activate && uvicorn main:app --reload",
    "client": "cd client && npm run dev",
    "install": "cd client && npm install",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}